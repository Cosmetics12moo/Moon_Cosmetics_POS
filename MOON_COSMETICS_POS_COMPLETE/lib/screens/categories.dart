import 'package:flutter/material.dart';
class CategoriesScreen extends StatelessWidget { const CategoriesScreen({super.key}); @override Widget build(BuildContext c)=>Scaffold(body:ListView(padding:const EdgeInsets.all(16),children:const[Card(child:ListTile(title:Text('Custom Categories'),subtitle:Text('Add, rename or deactivate your own categories'))),ListTile(title:Text('Makeup')),ListTile(title:Text('Skincare')),ListTile(title:Text('Hair Care')),ListTile(title:Text('Fragrance')),ListTile(title:Text('Personal Care'))]),floatingActionButton:FloatingActionButton.extended(onPressed:null,icon:const Icon(Icons.add),label:const Text('Add Category'))); }
